# Firebase Service Account JSON (stringified)
# Go to Firebase Console > Project Settings > Service Accounts > Generate New Private Key
# Copy the JSON content and paste it here as a single line
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"ntsmprotoan",...}

# Server secret for additional validation (optional)
SERVER_SECRET=your-random-secret-here
